# Projeto de App de Notas (Flutter + Firebase)

Este projeto usa Firebase Realtime Database.

## 🚀 Como Rodar

1.  Clone o repositório.
2.  Rode `flutter pub get`.
3.  Vá até o console do Firebase e crie seu próprio projeto.
4.  Rode `flutterfire configure` e selecione seu projeto para gerar o arquivo `lib/firebase_options.dart`.
5.  **Importante:** Adicione manualmente a `databaseURL` ao `firebase_options.dart`.
6.  Rode o app (`flutter run`).
